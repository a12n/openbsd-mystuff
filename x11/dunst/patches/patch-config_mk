$OpenBSD$
--- config.mk.orig	Wed Jul 30 00:16:43 2014
+++ config.mk	Tue Dec  1 00:20:04 2015
@@ -1,6 +1,6 @@
 # paths
 PREFIX ?= /usr/local
-MANPREFIX = ${PREFIX}/share/man
+MANPREFIX = ${TRUEPREFIX}/man
 
 # In dist tarballs, the version is stored in the VERSION files.
 VERSION := '$(shell [ -f VERSION ] && cat VERSION)'
